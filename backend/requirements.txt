# AutoAB backend Python dependencies.
#
# Versions below are the ones the pipeline was verified against (see
# docs/VALIDATION.md). Install with:
#
#     python3 -m pip install --user -r backend/requirements.txt
#
# changeo and presto install console scripts (MakeDb.py, DefineClones.py,
# CreateGermlines.py, ...). On a --user install these land in the Python user
# base bin directory, which the Electron main process discovers automatically;
# see buildSpawnPath() in electron-app/src/main/backend-runner.ts.

biopython==1.86
pandas==2.3.2
numpy==2.3.2
matplotlib==3.10.7
scipy==1.16.3
airr==1.5.1

changeo==1.3.4
presto==0.7.6

# airr 1.5.1 still imports pkg_resources, which setuptools removed in 81.
# System Pythons usually carry an older setuptools so this goes unnoticed, but
# any clean environment (venv, or the runtime built by scripts/bundle-python.sh)
# fails at `import airr` without the pin.
setuptools<81
